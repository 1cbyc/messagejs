# MessageJS Environment Variables
# Copy this file to .env and fill in your values

# ===== Core Backend API =====
# Server port (default: 3001)
PORT=3001

# Database - Use Accelerate for queries (faster, connection pooling)
# Format: prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_API_KEY
DATABASE_URL=prisma+postgres://accelerate.prisma-data.net/?api_key=your-accelerate-api-key

# Direct Database URL - Use for migrations (DATABASE_URL is required for prisma migrate)
# Format: postgresql://user:password@host:port/database
DATABASE_URL_DIRECT=postgresql://user:password@host:5432/database

# Redis (matches docker-compose.yml)
REDIS_URL=redis://localhost:6379

# JWT Secret (generate a random string for production)
# Generate with: openssl rand -hex 32
JWT_SECRET=your-jwt-secret-here-change-in-production

# Encryption Key (must be exactly 32 hex characters for AES-256-GCM)
# Generate with: openssl rand -hex 16
ENCRYPTION_KEY=your-32-character-encryption-key

# Logging
LOG_LEVEL=info
NODE_ENV=development

# WhatsApp Webhook (you'll set this when configuring WhatsApp)
WHATSAPP_VERIFY_TOKEN=your-whatsapp-webhook-verify-token

# ===== Dashboard Frontend =====
# Base URL for the dashboard (default: http://localhost:3000)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# API endpoint for the dashboard to connect to
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1
